'''
Created on 22 Apr 2012

@author: Pure_
'''

import IRCConfig;
import Bot;

class Main(object):

    def __init__(self, selfparams):
        # config = IRCConfig('irc.json')
        # bot = Bot(self, hostname, port, 1024, config.getUser(), config.getNick(), config.getChannels())
        # Here we will also have to set up multiple 'Bot' objects depending on how many servers we have to connect to